{
  "name": "qanet-tts-pwa-project",
  "version": "1.0.0",
  "private": true,
  "description": "Production-ready PWA for iOS Text-to-Speech",
  "scripts": {
    "dev": "npm run backend:dev & npm run frontend:dev",
    "frontend:dev": "cd frontend && npm install && npm run dev",
    "backend:dev": "cd backend && npm install && npm run dev",
    "frontend:build": "cd frontend && npm install && npm run build",
    "backend:build": "cd backend && npm install && npm run build",
    "build": "npm run backend:build && npm run frontend:build",
    "frontend:preview": "cd frontend && npm run preview",
    "backend:start": "cd backend && npm run start:compiled"
  },
  "keywords": ["pwa", "tts", "ios", "text-to-speech"],
  "author": "",
  "license": "MIT"
}

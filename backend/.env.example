# TTS Engine - set one of these providers
# Supported: google, azure, aws, mock
TTS_PROVIDER=mock

# Google Cloud TTS (if using provider=google)
GOOGLE_APPLICATION_CREDENTIALS=
GOOGLE_CLOUD_PROJECT=

# Azure TTS (if using provider=azure)
AZURE_TTS_KEY=
AZURE_TTS_REGION=westus

# AWS Polly (if using provider=aws)
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_REGION=us-east-1

# Server
PORT=3001
NODE_ENV=development

# Backend API base URL (defaults to http://localhost:3001 if not set)
# For local development with Vite host (accessing from another machine on LAN):
# VITE_API_BASE=http://192.168.X.X:3001

# For production:
# VITE_API_BASE=https://your-backend-domain.com
